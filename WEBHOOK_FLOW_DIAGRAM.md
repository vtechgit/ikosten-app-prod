# Diagrama de Flujo - Sistema de Suscripciones con Webhooks

## 📱 Flujo de Compra Inicial

```
┌─────────────────┐
│  Usuario abre   │
│  modal planes   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────────┐
│ Detecta         │ YES  │ Carga productos  │
│ iOS/Android? ───┼─────►│ desde RevenueCat │
└────────┬────────┘      └────────┬─────────┘
         │ NO                     │
         │                        ▼
         │              ┌──────────────────┐
         │              │ Usuario compra   │
         │              │ con Face ID/PIN  │
         │              └────────┬─────────┘
         │                       │
         │                       ▼
         │              ┌──────────────────┐
         │              │ RevenueCat       │
         │              │ procesa compra   │
         │              └────────┬─────────┘
         │                       │
         ▼                       ▼
┌─────────────────┐    ┌──────────────────┐
│ Abre PayPal     │    │ App registra     │
│ (Web)           │    │ en backend       │
└─────────────────┘    └────────┬─────────┘
                                │
                                ▼
                       ┌──────────────────┐
                       │ user_rol = 1     │
                       │ (PREMIUM)        │
                       └──────────────────┘
```

## 🔔 Flujo de Cancelación/Expiración

```
┌──────────────────┐
│ Usuario cancela  │
│ en App Store o   │
│ Google Play      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Apple/Google     │
│ notifica a       │
│ RevenueCat       │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ RevenueCat envía webhook                 │
│ POST /api/webhooks/revenuecat            │
│                                          │
│ {                                        │
│   event: {                               │
│     type: "CANCELLATION",                │
│     app_user_id: "user_123",             │
│     product_id: "com.ikosten.premium"    │
│   }                                      │
│ }                                        │
└────────┬─────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│ Backend recibe   │
│ y verifica firma │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────┐
│ Controller procesa evento:   │
│                              │
│ 1. Busca usuario por ID      │
│ 2. Busca suscripción activa  │
│ 3. Actualiza estado          │
│ 4. Actualiza rol a FREE (0)  │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Base de datos actualizada:   │
│                              │
│ • user_rol = 0               │
│ • status = CANCELLED         │
│ • cancelled_date = now()     │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Usuario pierde acceso        │
│ premium en siguiente login   │
└──────────────────────────────┘
```

## 🔄 Flujo de Renovación

```
┌──────────────────┐
│ Fecha de         │
│ renovación       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Apple/Google     │
│ cobra            │
│ automáticamente  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐      ┌──────────────────┐
│ ¿Pago exitoso?   │ YES  │ RevenueCat envía │
└────────┬─────────┘─────►│ evento RENEWAL   │
         │ NO              └────────┬─────────┘
         │                          │
         ▼                          ▼
┌──────────────────┐      ┌──────────────────┐
│ RevenueCat envía │      │ Backend actualiza│
│ BILLING_ISSUE    │      │ • status = ACTIVE│
└────────┬─────────┘      │ • last_renewal   │
         │                └──────────────────┘
         │
         │ (Período de gracia)
         │
         ▼
┌──────────────────┐
│ RevenueCat envía │
│ EXPIRATION       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Backend:         │
│ • user_rol = 0   │
│ • status = EXPIRED│
└──────────────────┘
```

## 📊 Estados de Suscripción

```
┌───────────┐
│  ACTIVE   │◄──────────────────┐
└─────┬─────┘                   │
      │                         │
      │ Usuario cancela    UNCANCELLATION
      │                         │
      ▼                         │
┌───────────┐                   │
│ CANCELLED │───────────────────┘
└─────┬─────┘
      │
      │ Fin de período pagado
      │
      ▼
┌───────────┐
│  EXPIRED  │
└───────────┘

┌───────────┐
│  ACTIVE   │
└─────┬─────┘
      │
      │ Fallo de renovación
      │
      ▼
┌───────────┐
│  EXPIRED  │
└───────────┘
```

## 🔐 Verificación de Seguridad

```
┌──────────────────┐
│ Webhook recibido │
└────────┬─────────┘
         │
         ▼
┌───────────────────────────────┐
│ Verificar header:             │
│ x-revenuecat-signature        │
│                               │
│ HMAC-SHA256 del body          │
│ usando WEBHOOK_SECRET         │
└────────┬──────────────────────┘
         │
         ├─────► ✅ Firma válida
         │       │
         │       ▼
         │    Procesar evento
         │
         └─────► ❌ Firma inválida
                 │
                 ▼
              Rechazar (401)
```

## 🧪 Endpoints Disponibles

```
Producción:
POST /api/webhooks/revenuecat
- Endpoint real para RevenueCat
- Requiere firma válida (en producción)
- Procesa eventos en tiempo real

Testing:
POST /api/webhooks/revenuecat/test
- Endpoint de prueba para desarrollo
- NO requiere firma
- Simula eventos manualmente
- Útil para testing local
```

## 📈 Monitoreo y Logs

```
Webhook recibido:
🔔 RevenueCat webhook recibido: { type: 'CANCELLATION', app_user_id: '123' }

Procesamiento:
❌ Cancelación detectada para usuario: 123
✅ Usuario downgradeado a FREE: { userId: '123', newRole: 0 }

O en caso de error:
⚠️ No se encontró suscripción activa para: 123
❌ Error procesando webhook: User not found
```

## 🎯 Casos de Uso Comunes

### 1. Usuario cancela pero sigue usando hasta fin de mes
```
Día 1: Usuario cancela → status = CANCELLED, rol = 1 (mantiene acceso)
Día 30: Período termina → RevenueCat envía EXPIRATION
                        → Backend: status = EXPIRED, rol = 0
```

### 2. Falla renovación por tarjeta expirada
```
Día renovación: Falla pago → BILLING_ISSUE (se registra)
+ 3 días gracia: Sigue activo, rol = 1
Día +7: No hay pago → EXPIRATION → rol = 0
```

### 3. Usuario reactiva suscripción cancelada
```
Estado: CANCELLED
Usuario: Reactiva en App Store
RevenueCat: UNCANCELLATION → status = ACTIVE, rol = 1
```
