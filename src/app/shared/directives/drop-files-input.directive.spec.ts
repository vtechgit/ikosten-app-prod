import { DropFilesInputDirective } from './drop-files-input.directive';

describe('DropFilesInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DropFilesInputDirective();
    expect(directive).toBeTruthy();
  });
});
